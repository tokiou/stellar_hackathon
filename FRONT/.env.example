# FRONT has no private env vars.
# Use root .env.local / Vercel env vars for Next.

NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Do NOT put API keys here. Server-side keys belong in root .env.local or Vercel env vars.
